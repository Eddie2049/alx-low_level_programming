#include <stdio.h>

/**
 * main - entry point
 * program prints specific message
 * only uses printf
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
